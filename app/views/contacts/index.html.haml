%h2.heading Contacts

- if contacts.present?
  %table.table.table-hover.table-responsive
    %thead
      %tr
        %th Full Name
        %th Email
    %tbody
      - contacts.each do |contact|
        %tr
          %td= contact.full_name
          %td= contact.email

  = paginate(contacts)
- else
  .well.well-lg
    %h4.center You currently don't have any contacts
    %p.center To get started, click the button below to import some contacts.
    .center
      = link_to 'Import Contacts', new_batch_contact_path, class: 'btn btn-primary'
