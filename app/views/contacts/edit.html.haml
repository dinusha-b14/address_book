%h2.heading Edit Contact

= simple_form_for(@contact_form, html: { class: 'form-horizontal'}) do |f|
  = f.input :first_name
  = f.input :last_name
  = f.input :email
  = f.submit 'Update Contact', class: 'btn btn-primary'
